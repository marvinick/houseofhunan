<div class="container-fluid">
	<div class="row">
		<div class="jumbotron">

		</div>
		<% @menus.each do |menu| %>
			<%= menu.name %>
		<% end %>
		<%= link_to "New Menu", new_menu_path %>
	</div>
</div>